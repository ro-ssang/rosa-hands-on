apiVersion: v2
name: app-a
description: common library를 가져다 쓰는 앱
type: application
version: 0.1.0
appVersion: "1.0"

dependencies:
  - name: common
    version: "0.1.0"
    repository: "file://../common"
