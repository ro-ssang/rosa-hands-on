apiVersion: batch/v1
kind: Job
metadata:
  name: batch-hello
  namespace: rosa-lab
spec:
  completions: 5
  parallelism: 2
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: hello
          image: busybox:1.37
          command:
            - sh
            - -c
            - 'echo "[$(date)] hello from $(hostname)"; sleep 5'
