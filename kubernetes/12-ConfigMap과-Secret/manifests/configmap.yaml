apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: rosa-lab
data:
  APP_NAME: rosa-lab-app
  LOG_LEVEL: info
  app.properties: |
    server.port=8080
    server.timeout=30s
    feature.beta=false
