apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: retain-claim
  namespace: rosa-lab
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Mi
  storageClassName: standard-retain
