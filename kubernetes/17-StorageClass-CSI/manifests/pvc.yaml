apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: auto-claim
  namespace: rosa-lab
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
  storageClassName: standard
