apiVersion: v1
kind: Pod
metadata:
  name: load
  namespace: rosa-lab
spec:
  containers:
    - name: load
      image: busybox:1.37
      command:
        - sh
        - -c
        - while sleep 0.01; do wget -q -O- http://php-apache > /dev/null; done
