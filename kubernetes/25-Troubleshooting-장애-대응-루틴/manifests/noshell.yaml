apiVersion: v1
kind: Pod
metadata:
  name: noshell
  namespace: rosa-lab
  labels:
    app: noshell
spec:
  containers:
    - name: app
      image: registry.k8s.io/pause:3.9   # 계속 도는데 shell·도구가 없는 이미지
