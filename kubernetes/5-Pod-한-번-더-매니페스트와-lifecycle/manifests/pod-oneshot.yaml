apiVersion: v1
kind: Pod
metadata:
  name: oneshot
  namespace: rosa-lab
spec:
  restartPolicy: Never
  containers:
    - name: echo
      image: busybox:1.37
      command: ["sh", "-c", "echo 'hello from oneshot'; sleep 2"]
